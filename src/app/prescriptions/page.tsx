import {PageHeader,PrescriptionCard} from "@/components/ui";import {prescriptionService} from "@/lib/services";
export default async function Prescriptions(){const data=await prescriptionService.getAll();return <div className="page"><PageHeader title="Prescriptions" subtitle="Review instructions shared by your practitioner."/><div className="flex gap-2 mb-5"><button className="pill bg-green-700 text-white">Active</button><button className="pill bg-white border">Previous</button></div><div className="space-y-4">{data.map(x=><PrescriptionCard item={x} key={x.id}/>)}</div><p className="muted text-xs mt-5">Fictional demonstration records. Follow advice from a qualified healthcare professional.</p></div>}
