import {HospitalCard,PageHeader,SearchInput} from "@/components/ui";import {hospitalService} from "@/lib/services";
export default async function Hospitals(){const data=await hospitalService.getAll();return <div className="page"><PageHeader title="Hospitals near you" subtitle="Explore care options around Awka, Anambra State."/><SearchInput placeholder="Search hospitals or services"/><div className="flex gap-2 my-4">{["Location","Services","Emergency care","Open now"].map(x=><button className="btn btn-outline text-sm" key={x}>{x}</button>)}</div><div className="grid lg:grid-cols-2 gap-4">{data.map(x=><HospitalCard hospital={x} key={x.id}/>)}</div></div>}
